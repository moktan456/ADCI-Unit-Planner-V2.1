rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // PUBLIC ACCESS - Firebase Authentication removed as requested
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
